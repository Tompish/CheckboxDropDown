import { ListmanagerDirective } from './listmanager.directive';

describe('ListmanagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ListmanagerDirective();
    expect(directive).toBeTruthy();
  });
});
